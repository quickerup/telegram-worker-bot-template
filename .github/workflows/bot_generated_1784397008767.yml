name: Bot Ping
on:
  workflow_dispatch:
  repository_dispatch:
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
    - name: Print message
      run: |
        echo "Cloudflare and AI integration is live!"